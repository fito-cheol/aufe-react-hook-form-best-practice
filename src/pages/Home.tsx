import React from 'react';

const Home: React.FC = () => {
  return (
    <div className="page">
      <h1>Welcome to React App</h1>
      <p>
        This is a modern React application built with Vite, TypeScript, and React Router.
        Navigate through the different pages using the navigation menu above.
      </p>
    </div>
  );
};

export default Home;
